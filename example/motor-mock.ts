
/*
import { Pin } from '../src';

const pin: Pin = new PinMock();

pin.write(true)
  .then(() => console.log('success'))
  .catch((error) => console.error(error));

const motor = new Motor(pin);

motor.forward()
  .then(() => console.log('test'))
  .catch((error) => console.error(error));


const pin: Pin = new PinMock();
const pin: Pin = new PinMock();

const scanner = new Scanner(pin);

scanner.read()
  .then((value) => console.log(value))
  .then((error) => console.log(error));
*/
